{
  "name": "xiaozhi-assistant",
  "version": "1.0.0",
  "description": "🚀 小智助手 - 智能提醒 + 资讯聚合机器人，你的贴心智能小管家",
  "main": "src/bot.js",
  "scripts": {
    "start": "NODE_ENV=production node src/bot.js",
    "dev": "set NODE_ENV=development && nodemon src/bot.js",
    "init-db": "node src/init-db.js",
    "crawl-news": "node src/scripts/crawlNews.js",
    "build": "set NODE_ENV=production esbuild src/bot.js --bundle --platform=node --external:pg --external:pg-hstore --external:mysql2 --external:mariadb --external:tedious --external:oracledb --outfile=dist/bot.js && esbuild src/init-db.js --bundle --platform=node --external:pg --external:pg-hstore --external:mysql2 --external:mariadb --external:tedious --external:oracledb --outfile=dist/init-db.js && esbuild src/scripts/crawlNews.js --bundle --platform=node --external:pg --external:pg-hstore --external:mysql2 --external:mariadb --external:tedious --external:oracledb --outfile=dist/crawlNews.js",
    "build:win": "esbuild src/bot.js --bundle --platform=node --external:pg --external:pg-hstore --external:mysql2 --external:mariadb --external:tedious --external:oracledb --outfile=dist/bot.js && esbuild src/init-db.js --bundle --platform=node --external:pg --external:pg-hstore --external:mysql2 --external:mariadb --external:tedious --external:oracledb --outfile=dist/init-db.js && esbuild src/scripts/crawlNews.js --bundle --platform=node --external:pg --external:pg-hstore --external:mysql2 --external:mariadb --external:tedious --external:oracledb --outfile=dist/crawlNews.js",
    "start:dist": "NODE_ENV=production node dist/bot.js",
    "start:dist:dev": "NODE_ENV=development node dist/bot.js",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:restart": "pm2 restart ecosystem.config.js",
    "pm2:stop": "pm2 stop ecosystem.config.js",
    "pm2:logs": "pm2 logs --lines 200"
  },
  "keywords": [
    "xiaozhi",
    "assistant",
    "telegram",
    "bot",
    "reminder",
    "news",
    "web3",
    "ai",
    "smart",
    "intelligent",
    "chatbot",
    "productivity",
    "time-management",
    "information-aggregation"
  ],
  "author": "小智助手团队",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/xiaozhi-assistant.git"
  },
  "homepage": "https://github.com/your-username/xiaozhi-assistant#readme",
  "bugs": {
    "url": "https://github.com/your-username/xiaozhi-assistant/issues"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.3",
    "node-telegram-bot-api": "^0.64.0",
    "sequelize": "^6.35.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "nodemon": "^3.0.2",
    "pm2": "^5.4.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
